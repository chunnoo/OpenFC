/*this.context.beginPath();
  this.context.moveTo(this.mouse.x + Math.cos(this.framecount/60)*50, this.mouse.y + Math.sin(this.framecount/60)*50);
  this.context.lineTo(this.mouse.x + Math.cos(this.framecount/60 + Math.PI*2/3)*50, this.mouse.y + Math.sin(this.framecount/60 + Math.PI*2/3)*50);
  this.context.lineTo(this.mouse.x + Math.cos(this.framecount/60 + Math.PI*4/3)*50, this.mouse.y + Math.sin(this.framecount/60 + Math.PI*4/3)*50);
  this.context.closePath();
  if (this.mouse.leftButton) {
    this.context.fillStyle = "#000";
    this.context.fill();
  } else {
    this.context.strokeStyle = "#000";
    this.context.stroke();
  }*/
